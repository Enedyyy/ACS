# FinTrack — Документация по файлам проекта

Приложение для учёта личных финансов с поддержкой группового бюджета.

**Стек:** Spring Boot 3.2 (Java 17), SQLite, Vue 3, Pinia, Vite, Chart.js

---

## Структура проекта

```
ACS/
├── src/main/java/com/acs/finance/    # Backend (Spring Boot)
├── web/src/                           # Frontend (Vue 3)
├── pom.xml                            # Maven конфигурация
└── data/                              # SQLite БД (fintrack.db)
```

---

# BACKEND (Java / Spring Boot)

---

## Точка входа

### `FinanceApplication.java`
**Путь:** `src/main/java/com/acs/finance/FinanceApplication.java`

Главный класс приложения Spring Boot.

- **Аннотации:**
  - `@SpringBootApplication` — включает авто-конфигурацию, сканирование компонентов
  - `@EnableScheduling` — включает планировщик для периодических задач (напоминания)
- **Метод `main()`** — запускает встроенный Tomcat на порту 8080

---

## Конфигурация

### `WebConfig.java`
**Путь:** `src/main/java/com/acs/finance/config/WebConfig.java`

Конфигурация веб-слоя Spring MVC.

**Функции:**
1. **`addResourceHandlers()`** — настраивает отдачу статических файлов из `web/public/` (фронтенд)
2. **`addCorsMappings()`** — разрешает CORS для API (`/api/**`):
   - Все origins (`*`)
   - Методы: GET, POST, PUT, DELETE, OPTIONS
   - Разрешены credentials (cookies)

### `application.properties`
**Путь:** `src/main/resources/application.properties`

Настройки приложения:
- **Порт:** 8080
- **БД:** SQLite (`./data/fintrack.db`)
- **Hibernate:** автоматическое создание/обновление таблиц (`ddl-auto=update`)
- **Connection pool:** Hikari (max 5 соединений)

---

## Контроллеры (REST API)

### `BaseController.java`
**Путь:** `src/main/java/com/acs/finance/controller/BaseController.java`

Абстрактный базовый контроллер — родитель для всех API-контроллеров.

**Методы:**
| Метод | Описание |
|-------|----------|
| `getSessionId(request)` | Извлекает session ID из cookie `SID` |
| `getUser(request)` | Возвращает пользователя по сессии (или `null`) |
| `requireAuth(request)` | Требует авторизацию, иначе бросает `UnauthorizedException` |
| `escapeJson(s)` | Экранирует строку для JSON (для SSE-сообщений) |

---

### `AuthController.java`
**Путь:** `src/main/java/com/acs/finance/controller/AuthController.java`

Управление аутентификацией пользователей.

**Эндпоинты:**
| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/register` | Регистрация (username, password) → создаёт пользователя |
| POST | `/api/login` | Вход → создаёт сессию, ставит cookie `SID` |
| POST | `/api/logout` | Выход → удаляет сессию, очищает cookie |
| GET | `/api/me` | Проверка текущей сессии → возвращает данные пользователя |

**Логика:**
- Пароли хешируются SHA-256 + Base64
- Session ID — случайный токен 24 байта (SecureRandom)
- Cookie `SID` — httpOnly, path=/

---

### `TransactionController.java`
**Путь:** `src/main/java/com/acs/finance/controller/TransactionController.java`

CRUD для финансовых транзакций.

**Эндпоинты:**
| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/transaction/add` | Добавить транзакцию (date, amount, category?, description?) |
| GET | `/api/transactions` | Список транзакций с фильтрами (from?, to?, category?) |
| POST | `/api/transaction/delete` | Удалить транзакцию по ID |

**Особенности:**
- При добавлении автоматически определяет категорию по описанию (AutoCategorizerService)
- Расходы (amount < 0) учитываются в бюджетах
- При превышении бюджета отправляется SSE-уведомление `alert`
- После изменений отправляются SSE-события `tx-added`, `budget-update`

---

### `BudgetController.java`
**Путь:** `src/main/java/com/acs/finance/controller/BudgetController.java`

Управление бюджетами по категориям.

**Эндпоинты:**
| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/budget/set` | Установить лимит для категории |
| GET | `/api/budget` | Получить все бюджеты пользователя |
| POST | `/api/budget/delete` | Удалить бюджет категории |

**Данные бюджета:**
- `category` — название категории
- `limit` — лимит расходов
- `spent` — уже потрачено (автоматически считается из транзакций)

---

### `ReminderController.java`
**Путь:** `src/main/java/com/acs/finance/controller/ReminderController.java`

Управление напоминаниями о платежах.

**Эндпоинты:**
| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/reminder/add` | Добавить напоминание (dueDate, message, amount?) |
| GET | `/api/reminders` | Список напоминаний пользователя |

**Примечание:** Напоминания обрабатываются планировщиком (ReminderScheduler) и отправляются через SSE.

---

### `GroupController.java`
**Путь:** `src/main/java/com/acs/finance/controller/GroupController.java`

Управление групповым бюджетом (семейный/совместный бюджет).

**Эндпоинты:**
| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/group/create` | Создать группу с названием |
| POST | `/api/group/join` | Присоединиться к группе по ID (+ доля участия) |
| GET | `/api/group/me` | Информация о текущей группе пользователя |
| GET | `/api/group/budget` | Агрегированный бюджет группы |
| GET | `/api/group/peers` | Статистика участников (доходы/расходы) |
| POST | `/api/group/leave` | Выйти из группы |

**Логика:**
- Пользователь может быть только в одной группе
- При присоединении указывается `share` (доля, 0.0-1.0)
- Бюджеты агрегируются с учётом долей участников
- Поддерживает присоединение по URL с параметром `?join=<groupId>`

---

### `CurrencyController.java`
**Путь:** `src/main/java/com/acs/finance/controller/CurrencyController.java`

Курсы валют и конвертация.

**Эндпоинты:**
| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/currency` | Курсы валют (base=MDL, symbols=USD,EUR,RUB,MDL) |
| GET | `/api/currency/convert` | Конвертация суммы (from, to, amount, date?) |

**Валюты:** MDL (базовая), USD, EUR, RUB

---

### `CategorizerController.java`
**Путь:** `src/main/java/com/acs/finance/controller/CategorizerController.java`

Автоматическое определение категории.

**Эндпоинт:**
| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/categorizer/suggest` | Предложить категорию по описанию |

---

### `SseController.java`
**Путь:** `src/main/java/com/acs/finance/controller/SseController.java`

Server-Sent Events для real-time уведомлений.

**Эндпоинт:**
| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/events` | SSE-соединение для получения событий |

**Типы событий:**
- `hello` — приветствие при подключении
- `tx-added` — добавлена транзакция
- `budget-update` — изменён бюджет
- `reminder` — сработало напоминание
- `alert` — превышен бюджет

---

### `HealthController.java`
**Путь:** `src/main/java/com/acs/finance/controller/HealthController.java`

Health-check эндпоинты.

**Эндпоинты:**
| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/health` | Проверка работоспособности сервера |
| GET | `/health/db` | Проверка подключения к БД |

---

## Сервисы (Бизнес-логика)

### `AuthService.java`
**Путь:** `src/main/java/com/acs/finance/service/AuthService.java`

Сервис аутентификации и управления сессиями.

**Функции:**
| Метод | Описание |
|-------|----------|
| `register(username, password)` | Регистрация нового пользователя |
| `login(username, password)` | Проверка учётных данных |
| `createSession(username)` | Создание сессии (токен + БД) |
| `destroySession(sid)` | Удаление сессии |
| `getUserBySession(sid)` | Получение пользователя по session ID |
| `getUserById(userId)` | Получение пользователя по ID |

**Оптимизации:**
- In-memory кэш пользователей (`ConcurrentHashMap`)
- In-memory кэш сессий для быстрого lookup

**Безопасность:**
- Пароли хешируются SHA-256
- Токены генерируются SecureRandom (24 байта, Base64 URL-safe)

---

### `FinanceService.java`
**Путь:** `src/main/java/com/acs/finance/service/FinanceService.java`

Основной сервис для работы с финансовыми данными.

**Методы:**
| Метод | Описание |
|-------|----------|
| `addTransaction(...)` | Добавление транзакции + обновление spent в бюджете |
| `listTransactions(...)` | Список транзакций с фильтрами |
| `deleteTransaction(...)` | Удаление транзакции + корректировка бюджета |
| `setBudget(...)` | Создание/обновление бюджета |
| `getBudgets(userId)` | Список бюджетов пользователя |
| `deleteBudget(...)` | Удаление бюджета |
| `addReminder(...)` | Добавление напоминания |
| `getReminders(userId)` | Список напоминаний |
| `consumeDueReminders(...)` | Получение и пометка отправленных напоминаний |
| `round2(v)` | Округление до 2 знаков (статический метод) |

---

### `GroupService.java`
**Путь:** `src/main/java/com/acs/finance/service/GroupService.java`

Сервис для работы с группами.

**Методы:**
| Метод | Описание |
|-------|----------|
| `create(name)` | Создание группы |
| `join(userId, groupId, share)` | Присоединение к группе |
| `leave(userId)` | Выход из группы |
| `userGroupId(userId)` | ID группы пользователя |
| `members(groupId)` | Участники группы с долями |
| `myShare(userId)` | Доля пользователя в группе |
| `getName(groupId)` | Название группы |

**Логика:**
- При присоединении к новой группе автоматически покидает старую
- Пустые группы автоматически удаляются

---

### `CurrencyService.java`
**Путь:** `src/main/java/com/acs/finance/service/CurrencyService.java`

Сервис курсов валют.

**Методы:**
| Метод | Описание |
|-------|----------|
| `getRates()` | Получение текущих курсов (с кэшированием) |
| `convert(from, to, amount, date)` | Конвертация суммы |
| `historyOnDate(...)` | Исторические курсы на дату |

**Внешний API:** `api.exchangerate.host`

**Кэширование:**
- Текущие курсы: TTL 1 час
- Исторические: TTL 6 часов
- При ошибке API используются fallback-значения (stub)

---

### `AutoCategorizerService.java`
**Путь:** `src/main/java/com/acs/finance/service/AutoCategorizerService.java`

Автоматическое определение категории по описанию.

**Логика:**
- Словарь ключевых слов → категорий
- Поиск подстроки (case-insensitive)

**Предустановленные правила:**
| Ключевое слово | Категория |
|----------------|-----------|
| еда, ресторан, кафе, market, supermarket, alimentara | Питание |
| такси, transport, bus | Транспорт |
| аренда, квартира, rent | Жильё |
| зарплата, salary | Доход |
| аптека, pharmacy | Здоровье |

---

### `SseService.java`
**Путь:** `src/main/java/com/acs/finance/service/SseService.java`

Управление SSE-соединениями.

**Методы:**
| Метод | Описание |
|-------|----------|
| `register(sessionId)` | Регистрация нового SSE-соединения |
| `unregister(sessionId)` | Закрытие соединения |
| `send(sessionId, json)` | Отправка события конкретному пользователю |
| `broadcast(sessionIds, json)` | Отправка события нескольким пользователям |
| `sessionIds()` | Список активных сессий |

**Хранение:** `ConcurrentHashMap<sessionId, SseEmitter>`

---

## Планировщик

### `ReminderScheduler.java`
**Путь:** `src/main/java/com/acs/finance/scheduler/ReminderScheduler.java`

Фоновая задача для отправки напоминаний.

**Логика:**
- Запускается каждые 10 секунд (`@Scheduled(fixedRate = 10000)`)
- Проверяет все активные SSE-сессии
- Для каждого пользователя находит просроченные напоминания
- Отправляет SSE-событие `reminder` с текстом и суммой
- Помечает напоминания как отправленные

---

## Сущности (JPA Entities)

### `User.java`
**Таблица:** `users`

| Поле | Тип | Описание |
|------|-----|----------|
| id | String (UUID) | Первичный ключ |
| username | String | Уникальное имя пользователя |
| passwordHash | String | Хеш пароля (SHA-256) |
| groupId | String | ID группы (опционально) |
| share | Double | Доля в группе (по умолчанию 1.0) |

---

### `Session.java`
**Таблица:** `sessions`

| Поле | Тип | Описание |
|------|-----|----------|
| sid | String | Session ID (первичный ключ) |
| username | String | Имя пользователя |
| createdAt | Instant | Время создания |

---

### `Transaction.java`
**Таблица:** `transactions`

| Поле | Тип | Описание |
|------|-----|----------|
| id | String (UUID) | Первичный ключ |
| userId | String | ID пользователя |
| dateEpochDay | Long | Дата (epoch day) |
| category | String | Категория (опционально) |
| description | String | Описание (опционально) |
| amount | BigDecimal | Сумма (+ доход, - расход) |

---

### `Budget.java`
**Таблица:** `budgets`

| Поле | Тип | Описание |
|------|-----|----------|
| id | BudgetId | Составной ключ (userId + category) |
| limitAmount | BigDecimal | Лимит расходов |
| spent | BigDecimal | Потрачено (автоматически обновляется) |

### `BudgetId.java`
**Embeddable** составной ключ для Budget.

| Поле | Тип |
|------|-----|
| userId | String |
| category | String |

---

### `Reminder.java`
**Таблица:** `reminders`

| Поле | Тип | Описание |
|------|-----|----------|
| id | String (UUID) | Первичный ключ |
| userId | String | ID пользователя |
| dueEpochDay | Long | Дата напоминания |
| message | String | Текст напоминания |
| amount | BigDecimal | Сумма (опционально) |
| sent | Boolean | Отправлено ли уведомление |

---

### `Group.java`
**Таблица:** `groups`

| Поле | Тип | Описание |
|------|-----|----------|
| id | String (UUID) | Первичный ключ |
| name | String | Название группы |

---

### `GroupMember.java`
**Таблица:** `group_members`

| Поле | Тип | Описание |
|------|-----|----------|
| id | GroupMemberId | Составной ключ |
| share | BigDecimal | Доля участника (0.0-1.0) |

### `GroupMemberId.java`
**Embeddable** составной ключ для GroupMember.

| Поле | Тип |
|------|-----|
| groupId | String |
| userId | String |

---

## Репозитории (JPA Repositories)

### `UserRepository.java`
- `findByUsername(username)` — поиск по имени
- `existsByUsername(username)` — проверка существования

### `SessionRepository.java`
- `findBySid(sid)` — поиск по session ID
- `deleteBySid(sid)` — удаление сессии

### `TransactionRepository.java`
- `findByUserIdOrderByDateEpochDayAsc(userId)` — все транзакции пользователя
- `findFiltered(userId, from, to, category)` — с фильтрами
- `deleteByIdAndUserId(id, userId)` — безопасное удаление
- `existsByIdAndUserId(id, userId)` — проверка владельца

### `BudgetRepository.java`
- `findByUserId(userId)` — все бюджеты пользователя
- `findByUserIdAndCategory(userId, category)` — конкретный бюджет
- `addSpent(userId, category, amount)` — инкремент spent
- `deleteByUserIdAndCategory(userId, category)` — удаление

### `ReminderRepository.java`
- `findByUserId(userId)` — все напоминания
- `findDueReminders(userId, today)` — просроченные неотправленные
- `markAsSent(id)` — пометить как отправленное

### `GroupRepository.java`
- Стандартные CRUD операции

### `GroupMemberRepository.java`
- `findByGroupId(groupId)` — участники группы
- `findByUserId(userId)` — группа пользователя
- `findGroupIdByUserId(userId)` — только ID группы
- `deleteByUserId(userId)` — выход из группы
- `countByGroupId(groupId)` — количество участников

---

## Исключения

### `UnauthorizedException.java`
**HTTP 401 Unauthorized**

Бросается при попытке доступа к защищённому ресурсу без авторизации.

### `GlobalExceptionHandler.java`
**@ControllerAdvice** — глобальный обработчик исключений.

| Исключение | HTTP код | Ответ |
|------------|----------|-------|
| UnauthorizedException | 401 | `{"error": "unauthorized"}` |
| IllegalArgumentException | 400 | `{"ok": false, "error": "..."}` |
| Exception (любое) | 500 | `{"error": "server_error"}` |

---

## Модели

### `CurrencyRates.java`
**Путь:** `src/main/java/com/acs/finance/model/CurrencyRates.java`

DTO для хранения курсов валют.

| Поле | Тип | Описание |
|------|-----|----------|
| base | String | Базовая валюта (MDL) |
| rates | Map<String, Double> | Курсы к другим валютам |
| timestamp | long | Время получения |

---

# FRONTEND (Vue 3 / TypeScript)

---

## Конфигурация

### `package.json`
**Путь:** `web/package.json`

**Зависимости:**
- `vue` 3.5 — UI фреймворк
- `pinia` 2.2 — state management
- `chart.js` 4.4 — графики

**Скрипты:**
- `npm run dev` — dev сервер (Vite)
- `npm run build` — production сборка

### `vite.config.ts`
Конфигурация Vite для сборки Vue приложения.

### `tsconfig.json`
Конфигурация TypeScript.

---

## Точка входа

### `main.ts`
**Путь:** `web/src/main.ts`

Создаёт Vue приложение, подключает Pinia store, монтирует в `#app`.

---

## Главный компонент

### `App.vue`
**Путь:** `web/src/App.vue`

Корневой компонент приложения.

**Структура:**
1. **Header** — шапка с логотипом и меню пользователя
2. **AuthForm** — форма входа/регистрации (для неавторизованных)
3. **Dashboard** — основной контент:
   - StatsCards — статистика (всего/расходы/доходы)
   - TransactionForm — форма добавления транзакции
   - ChartView — графики по категориям/группе
   - BudgetManager — управление бюджетами
   - CurrencyConverter — курсы валют
   - TransactionList — история транзакций
   - Recommendations — рекомендации по экономии
   - Reminders — напоминания
   - GroupBudget — групповой бюджет
   - CryptoView — курсы криптовалют

**Инициализация:**
1. Применяет тему из localStorage
2. Проверяет параметр `?join=` для присоединения к группе
3. Проверяет текущую сессию (`/api/me`)
4. Если авторизован — подключает SSE и загружает данные

---

## Store (Pinia)

### `store/index.ts`
**Путь:** `web/src/store/index.ts`

Глобальное состояние приложения.

**State:**
| Поле | Тип | Описание |
|------|-----|----------|
| user | User \| null | Текущий пользователь |
| favoritesSet | Set<string> | Избранные категории |
| theme | string | Тема (dark/light) |
| baseCurrency | string | Базовая валюта |

**Actions:**
- `setUser(u)` — установить пользователя
- `toggleFavorite(cat)` — добавить/убрать из избранного
- `setTheme(t)` — сменить тему
- `setBaseCurrency(c)` — сменить базовую валюту

**Персистентность:** favorites, theme, baseCurrency сохраняются в localStorage.

---

## API клиент

### `api/client.ts`
**Путь:** `web/src/api/client.ts`

HTTP клиент для работы с backend API.

**Функции:**
| Функция | Описание |
|---------|----------|
| `api(path, opts)` | Универсальный HTTP запрос с кэшированием |
| `sseConnect(onEvent)` | Подключение к SSE для событий |
| `suggestCategory(desc)` | Запрос автокатегоризации |
| `exportTransactionsCsv(items)` | Генерация CSV файла |

**Кэширование (IndexedDB):**
- GET запросы кэшируются на 5 минут
- POST/PUT/DELETE инвалидируют связанный кэш
- При offline возвращаются закэшированные данные

---

## Composables (Vue Composition API)

### `useAuth.ts`
**Путь:** `web/src/composables/useAuth.ts`

Логика аутентификации.

**Возвращает:**
| Свойство/Метод | Описание |
|----------------|----------|
| `user` | Computed текущего пользователя |
| `login(form)` | Вход в систему |
| `register(form)` | Регистрация |
| `logout()` | Выход |
| `afterAuth()` | Действия после успешного входа (SSE, загрузка данных) |

---

### `useTransactions.ts`
**Путь:** `web/src/composables/useTransactions.ts`

Управление транзакциями.

**State:**
- `transactions` — список транзакций
- `filters` — фильтры (from, to, category, search, onlyFav)
- `stats` — статистика (total, expenses, income)
- `txForm` — форма добавления
- `templates` — сохранённые шаблоны

**Методы:**
| Метод | Описание |
|-------|----------|
| `refreshTx()` | Загрузить транзакции с сервера |
| `addTransaction()` | Добавить транзакцию |
| `deleteTx(id)` | Удалить транзакцию |
| `setQuick(kind)` | Быстрый фильтр (сегодня/неделя/месяц) |
| `exportCsv()` | Экспорт в CSV |
| `saveTemplate(name)` | Сохранить шаблон |
| `useTemplate(name)` | Применить шаблон |
| `maybeSuggestCategory()` | Автоопределение категории |

---

### `useBudgets.ts`
**Путь:** `web/src/composables/useBudgets.ts`

Управление бюджетами.

**Методы:**
| Метод | Описание |
|-------|----------|
| `refreshBudget()` | Загрузить бюджеты |
| `setBudget(form)` | Установить лимит |
| `deleteBudget(category)` | Удалить лимит |

---

### `useReminders.ts`
**Путь:** `web/src/composables/useReminders.ts`

Управление напоминаниями.

**Методы:**
| Метод | Описание |
|-------|----------|
| `refreshReminders()` | Загрузить напоминания |
| `addReminder(form)` | Добавить напоминание |

---

### `useGroup.ts`
**Путь:** `web/src/composables/useGroup.ts`

Управление групповым бюджетом.

**State:**
- `groupBudget` — бюджеты группы
- `groupPeers` — участники группы
- `myGroup` — информация о текущей группе

**Методы:**
| Метод | Описание |
|-------|----------|
| `groupCreate(name)` | Создать группу |
| `groupJoin(groupId)` | Присоединиться |
| `groupLeave()` | Выйти из группы |
| `copyInvite()` | Скопировать ссылку-приглашение |
| `refreshGroupMeta()` | Обновить данные группы |

---

### `useCurrency.ts`
**Путь:** `web/src/composables/useCurrency.ts`

Курсы валют и конвертация.

**State:**
- `currency` — текущий курс (base, symbol, val)
- `convert` — данные конвертации (amount, result)

**Методы:**
| Метод | Описание |
|-------|----------|
| `refreshRate()` | Загрузить курс |
| `convertCurrency()` | Выполнить конвертацию |

---

### `useChart.ts`
**Путь:** `web/src/composables/useChart.ts`

Управление графиками (Chart.js).

**State:**
- `viewMode` — режим (categories/group)
- `chartMode` — тип данных (expenses/income/both)

**Методы:**
| Метод | Описание |
|-------|----------|
| `refreshChart()` | Перерисовать график |
| `handleChartRefresh()` | Обновить данные и график |

**Графики:**
- Doughnut chart по категориям расходов
- При режиме "group" — по участникам группы

---

### `useRecommendations.ts`
**Путь:** `web/src/composables/useRecommendations.ts`

Генерация финансовых рекомендаций.

**Настраиваемые параметры (recOpts):**
| Параметр | По умолчанию | Описание |
|----------|--------------|----------|
| periodDays | 30 | Период анализа |
| subWindowDays | 60 | Окно поиска подписок |
| subMinCount | 3 | Мин. повторов для подписки |
| subMinSum | 100 | Мин. сумма подписки |
| topSharePct | 35 | Порог доминирующей категории |
| top1CutPct | 12 | % сокращения топ-1 категории |
| top2CutPct | 10 | % сокращения топ-2 категории |

**Типы рекомендаций:**
1. Расходы > доходов
2. Оптимизация топ категорий
3. Обнаружение подписок
4. Превышение бюджетов
5. Предложение лимитов для категорий без бюджета

---

### `useCrypto.ts`
**Путь:** `web/src/composables/useCrypto.ts`

Курсы криптовалют.

**API:** CoinGecko (бесплатный)

**Монеты:** BTC, ETH, BNB, SOL, TON, DOGE, ADA, XRP, DOT

**Кэширование:** localStorage (при ошибке API)

---

### `useToast.ts`
**Путь:** `web/src/composables/useToast.ts`

Уведомления (toast).

**Методы:**
- `toast(msg)` — показать сообщение на 2.5 секунды

---

## Компоненты Vue

### `Header.vue`
**Путь:** `web/src/components/Header.vue`

Шапка приложения.

**Содержит:**
- Логотип "💰 FinTrack"
- Меню пользователя (если авторизован):
  - Переключение темы
  - Кнопка выхода
- Кнопка "Войти" (для неавторизованных)

---

### `AuthForm.vue`
**Путь:** `web/src/components/AuthForm.vue`

Форма авторизации.

**Поля:**
- Логин (мин. 3 символа)
- Пароль (мин. 3 символа)

**Действия:**
- Войти — `@login`
- Зарегистрироваться — `@register`

---

### `StatsCards.vue`
**Путь:** `web/src/components/StatsCards.vue`

Карточки статистики.

**Показывает:**
- Всего транзакций
- Сумма расходов
- Сумма доходов

---

### `TransactionForm.vue`
**Путь:** `web/src/components/TransactionForm.vue`

Форма добавления транзакции.

**Поля:**
- Дата
- Тип (расход/доход)
- Сумма
- Категория (+ кнопка избранного ★)
- Описание (авто-подсказка категории)

**Фичи:**
- Быстрые суммы: -100, -500, -1000, +100, +500, +1000
- Шаблоны: выбор, сохранение, удаление

---

### `TransactionList.vue`
**Путь:** `web/src/components/TransactionList.vue`

Таблица истории транзакций.

**Фильтры:**
- Быстрые: Сегодня, Неделя, Месяц
- Поиск по описанию
- Только избранные категории
- Диапазон дат (от/до)
- Категория

**Колонки:** Дата, Категория, Описание, Сумма, Удалить

**Действия:**
- Экспорт CSV
- Удаление транзакции

---

### `BudgetManager.vue`
**Путь:** `web/src/components/BudgetManager.vue`

Управление бюджетами.

**Форма:** Категория + Лимит → Установить

**Список бюджетов:** Категория: потрачено / лимит [Удалить]

---

### `ChartView.vue`
**Путь:** `web/src/components/ChartView.vue`

Компонент графиков.

**Настройки:**
- Режим: Категории / Группа
- Показать: Расходы / Доходы / Вместе

**График:** Doughnut (Chart.js)

---

### `CurrencyConverter.vue`
**Путь:** `web/src/components/CurrencyConverter.vue`

Конвертер валют.

**Поля:**
- База (MDL/USD/EUR/RUB)
- К валюте (USD/EUR/RUB/MDL)
- Сумма

**Отображает:**
- Текущий курс: 1 BASE = X SYMBOL
- Результат конвертации

---

### `Reminders.vue`
**Путь:** `web/src/components/Reminders.vue`

Управление напоминаниями.

**Форма:** Дата + Сообщение + Сумма (опц.) → Добавить

**Список:** Дата: Сообщение (сумма)

---

### `Recommendations.vue`
**Путь:** `web/src/components/Recommendations.vue`

Финансовые рекомендации.

**Настройки:** (раскрываемая панель)
- Период анализа
- Параметры поиска подписок
- Пороги сокращения

**Список рекомендаций:**
- Текст рекомендации
- Потенциальная экономия
- Обоснование (критерий)

---

### `GroupBudget.vue`
**Путь:** `web/src/components/GroupBudget.vue`

Управление групповым бюджетом.

**Формы:**
- Создать группу (название)
- Присоединиться (ID группы)

**Информация:**
- Текущая группа
- Таблица участников (пользователь, доход, расход)

**Действия:**
- Скопировать приглашение
- Выйти из группы
- Обновить данные

---

### `CryptoView.vue`
**Путь:** `web/src/components/CryptoView.vue`

Курсы криптовалют.

**Настройки:** Валюта отображения (USD/EUR)

**Таблица:** Монета, Цена, Изменение 24ч (%)

---

## Утилиты

### `utils/format.ts`
**Путь:** `web/src/utils/format.ts`

Форматирование чисел.

**Функции:**
- `fmt(n)` — форматирование числа в ru-RU локали (2 знака после запятой)

---

## Стили

### `public/styles.css`
**Путь:** `web/public/styles.css`

Глобальные CSS стили приложения.

**Темы:**
- Dark (по умолчанию) — тёмно-зелёные тона
- Light — светлые тона

**Компоненты:**
- `.topbar` — липкая шапка с blur
- `.tile` — карточки с градиентом и hover-эффектами
- `.stat-card` — карточки статистики
- `.form-grid` — сетка формы
- `.table-container` — таблицы
- `.toast` — уведомления
- Анимации: fadeUp, slideIn, gridFadeIn, pulse

**Адаптивность:**
- < 1200px: колонки сетки становятся на всю ширину
- < 768px: формы в одну колонку

---

## Публичные файлы

### `public/sw.js`
Service Worker (для offline режима / PWA).

### `public/index.html`
HTML шаблон для production сборки.

---

# Зависимости (pom.xml)

| Зависимость | Версия | Назначение |
|-------------|--------|------------|
| spring-boot-starter-web | 3.2.4 | REST API, MVC |
| spring-boot-starter-data-jpa | 3.2.4 | ORM (Hibernate) |
| spring-boot-starter-validation | 3.2.4 | Валидация |
| spring-boot-starter-webflux | 3.2.4 | SSE поддержка |
| sqlite-jdbc | 3.45.1.0 | SQLite драйвер |
| hibernate-community-dialects | 6.4.4 | SQLite диалект |
| lombok | edge-SNAPSHOT | Генерация boilerplate |
| jakarta.validation-api | — | API валидации |

---

# Запуск проекта

## Backend
```bash
cd ACS
mvn spring-boot:run
```
Сервер запустится на http://localhost:8080

## Frontend (dev)
```bash
cd ACS/web
npm install
npm run dev
```
Dev сервер на http://localhost:5173 (проксирует API на 8080)

## Production
```bash
cd ACS/web
npm run build
# Собранные файлы в web/public/ автоматически раздаются Spring Boot
cd ..
mvn spring-boot:run
```
Всё приложение на http://localhost:8080

---

*Документация создана автоматически на основе анализа исходного кода.*
