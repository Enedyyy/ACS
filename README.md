# FinTrack — Система финансового учёта

Клиент-серверное приложение для личного и семейного финансового учёта с поддержкой офлайн-режима, групповых бюджетов, рекомендаций и аналитики.

---

## Технологический стек

### Бэкенд
- **Язык**: Java 11+ (рекомендуется 17+)
- **HTTP-сервер**: `com.sun.net.httpserver.HttpServer` (встроенный в JDK)
- **База данных**: PostgreSQL (опционально, через JDBC) или in-memory хранение
- **Многопоточность**: `ExecutorService` (пул потоков), `ScheduledExecutorService` (периодические задачи)
- **Реалтайм**: Server-Sent Events (SSE) для push-уведомлений
- **Авторизация**: Cookie-сессии (`SID`), SHA-256 хеширование паролей

### Фронтенд
- **Фреймворк**: Vue 3 (Composition API, `<script setup>`)
- **Сборщик**: Vite
- **Язык**: TypeScript
- **Графики**: Chart.js (Doughnut, Line)
- **PWA**: Service Worker для офлайн-кэширования
- **Хранилище**: IndexedDB (кэш API-ответов), LocalStorage (настройки, шаблоны, избранное)
- **Стили**: CSS с CSS-переменными для темной/светлой темы

### Внешние API
- **Валюты**: `exchangerate.host`, `frankfurter.app` (фолбэк)
- **Криптовалюты**: CoinGecko API (клиентская сторона)

---

## Архитектура

### Общая схема
```
┌─────────────────┐
│   Браузер       │
│  (Vue 3 SPA)    │◄───┐
└────────┬────────┘    │
         │ HTTP/REST    │ SSE
         │              │
┌────────▼──────────────▼──┐
│  Java HttpServer         │
│  (порт 8080)             │
│  - REST API (/api/*)     │
│  - SSE (/api/events)     │
│  - Статика (web/public/) │
└────────┬─────────────────┘
         │
    ┌────┴────┐
    │         │
┌───▼───┐ ┌──▼────────┐
│ In-   │ │ PostgreSQL│
│ Memory│ │ (JDBC)    │
└───────┘ └───────────┘
```

### Потоки данных
1. **Авторизация**: POST `/api/login` → cookie `SID` → все последующие запросы с cookie
2. **Транзакции**: POST `/api/transaction/add` → сервер сохраняет → SSE событие `tx-added` → фронт обновляет графики/историю
3. **Офлайн-режим**: Service Worker кэширует статику, IndexedDB кэширует GET-ответы, при восстановлении связи — синхронизация
4. **Реалтайм**: EventSource подключается к `/api/events`, получает события (транзакции, бюджеты, напоминания, алерты)

---

## Подробное описание UI блоков

### 1. **Шапка (Header)**
**Файл**: `web/src/App.vue` (строки 2-12)

**Элементы**:
- Логотип "💰 FinTrack"
- Переключатель темы (🌓) — темная/светлая
- Имя пользователя (если авторизован)
- Кнопки "Войти" / "Выйти"

**Технологии**: Vue 3 `computed`, `v-if`, `@click`, CSS Grid/Flexbox

---

### 2. **Блок авторизации**
**Файл**: `web/src/App.vue` (строки 15-23)

**Функционал**:
- Форма входа (логин/пароль)
- Переключение на регистрацию
- Валидация (минимум 3 символа)

**API**: POST `/api/login`, POST `/api/register`

**Логика**: После успешного входа — сохранение пользователя в store, установка cookie, переход к дашборду

---

### 3. **Статистика (Stats Row)**
**Файл**: `web/src/App.vue` (строки 26-39)

**Показывает**:
- Всего транзакций
- Сумма расходов (отрицательные суммы)
- Сумма доходов (положительные суммы)

**Вычисление**: Клиентская агрегация из `transactions.value` (Vue `computed`)

---

### 4. **Блок "Добавить транзакцию"**
**Файл**: `web/src/App.vue` (строки 42-77)

**Поля формы**:
- Дата (date picker)
- Тип: Расход / Доход (select)
- Сумма (number input)
- Категория (text input) + кнопка избранного (★/☆)
- Описание (text input) — автоподсказка категории при вводе

**Дополнительные функции**:
- **Быстрые суммы**: кнопки -100, -500, -1000, +100, +500, +1000 (устанавливают сумму и тип)
- **Шаблоны транзакций**:
  - Выбор шаблона из списка → кнопка "Подставить" (заполняет тип, категорию, описание = название шаблона)
  - Сохранение текущей формы как шаблона (имя + поля)
  - Удаление шаблона
  - Хранение: LocalStorage (`fin_templates`)

**API**: POST `/api/transaction/add`

**Логика**: После сохранения — обновление истории, графиков, статистики, рекомендаций, отправка SSE события

---

### 5. **Блок "Графики"**
**Файл**: `web/src/App.vue` (строки 79-99)

**Режимы отображения**:
- **Режим "Категории"**: Doughnut-диаграмма расходов/доходов по категориям (Chart.js)
- **Режим "Группа"**: Doughnut-диаграмма доходов/расходов по участникам группы

**Настройки**:
- Выбор режима (categories/group)
- Выбор типа данных (expenses/income/both)
- Кнопка "Обновить"

**Технологии**: Chart.js (`chart.js/auto`), Canvas API

**Логика**: `refreshChart()` агрегирует данные из `transactions.value` или `groupPeers.value`, строит Doughnut-диаграмму, обновляется при изменении данных или переключении режима

---

### 6. **Блок "Бюджеты"**
**Файл**: `web/src/App.vue` (строки 101-116)

**Функционал**:
- Форма установки лимита (категория + лимит)
- Список бюджетов: `категория: потрачено / лимит`
- Кнопка удаления лимита (🗑)

**API**: POST `/api/budget/set`, GET `/api/budget`, POST `/api/budget/delete`

**Логика**: При добавлении расхода сервер автоматически увеличивает `spent` для соответствующей категории. При превышении лимита — SSE событие `budget-update` с флагом превышения, рекомендация в блоке "Рекомендации"

---

### 7. **Блок "Курсы валют"**
**Файл**: `web/src/App.vue` (строки 118-143)

**Функционал**:
- Выбор базовой валюты (MDL/USD/EUR/RUB)
- Выбор целевой валюты
- Поле ввода суммы для конвертации
- Кнопка "Пересчитать"
- Отображение текущего курса и результата конвертации

**API**: GET `/api/currency?base=...&symbols=...`, GET `/api/currency/convert?from=...&to=...&amount=...`

**Технологии**: Внешние API (`exchangerate.host`, `frankfurter.app`), кэширование на сервере (1 минута)

**Логика**: Сервер получает курс из внешнего API (с фолбэком), кэширует, возвращает JSON. Конвертация: `amount * rate`, форматирование до 2 знаков после запятой

---

### 8. **Блок "История транзакций"**
**Файл**: `web/src/App.vue` (строки 145-293)

**Функционал**:
- **Быстрые фильтры**: Сегодня / Неделя / Месяц
- **Ручные фильтры**: Дата от/до, категория, поиск по описанию
- **Фильтр по избранным категориям**: чекбокс "Только избранные"
- **Таблица транзакций**:**
  - Колонки: Дата, Тип, Сумма, Категория, Описание
  - Кнопка удаления (🗑) для каждой транзакции
- **Экспорт CSV**: кнопка "Экспорт CSV"

**API**: GET `/api/transactions?from=...&to=...&category=...`, POST `/api/transaction/delete`

**Логика**: Фильтрация на клиенте после загрузки, удаление с подтверждением, обновление всех зависимых блоков после удаления

---

### 9. **Блок "Рекомендации"**
**Файл**: `web/src/App.vue` (строки 295-370)

**Функционал**:
- Кнопка "⚙ Показать настройки" (переключает видимость настроек)
- **Настройки рекомендаций** (LocalStorage `fin_rec_opts`):
  - Период анализа (дней) — по умолчанию 30
  - Окно для подписок (дней) — по умолчанию 60
  - Повторения подписки (раз) — по умолчанию 3
  - Порог суммы подписки — по умолчанию 100
  - Сократить топ-категорию (%) — по умолчанию 12
  - Сократить вторую категорию (%) — по умолчанию 10
  - Считать доминирующей, если доля > (%) — по умолчанию 35
- **Список рекомендаций** с указанием основания и потенциальной экономии

**Типы рекомендаций**:
1. Расходы превышают доходы (за период)
2. Оптимизация топ-1 категории (сократить на %)
3. Оптимизация топ-2 категории (сократить на %)
4. Возможные подписки (повторяющиеся описания)
5. Превышен бюджет по категории
6. Слишком высокая концентрация в одной категории (>35%)
7. Нет бюджета на топ-3 категории

**Логика**: Клиентская функция `buildRecommendations()` анализирует `transactions.value` и `budgets.value`, применяет настройки, строит список рекомендаций. Обновляется при изменении транзакций/бюджетов

---

### 10. **Блок "Напоминания"**
**Файл**: `web/src/App.vue` (строки 372-390)

**Функционал**:
- Форма добавления: дата, сообщение, сумма (опционально)
- Список напоминаний (дата, сообщение, сумма)
- Автоматические уведомления через SSE при наступлении даты

**API**: POST `/api/reminder/add`, GET `/api/reminders`

**Логика**: Сервер каждые 10 секунд проверяет наступившие напоминания, отправляет SSE событие `reminder`, фронт показывает toast и обновляет список

---

### 11. **Блок "Семейный бюджет" (Группы)**
**Файл**: `web/src/App.vue` (строки 392-450)

**Функционал**:
- **Создание группы**: название → POST `/api/group/create` → получение `groupId`
- **Присоединение к группе**: ввод `groupId` → POST `/api/group/join`
- **Отображение информации о группе**:
  - ID группы
  - Кнопка "Скопировать приглашение" (генерирует URL с `?join=<groupId>`)
  - Кнопка "Выйти из группы" (POST `/api/group/leave`)
- **Таблица участников**: колонки "Участник", "Доходы", "Расходы" (агрегация за период)
- **График группы**: в блоке "Графики" при выборе режима "Группа" — Doughnut по участникам

**API**: POST `/api/group/create`, POST `/api/group/join`, POST `/api/group/leave`, GET `/api/group/me`, GET `/api/group/peers`

**Логика**: Группы хранятся в БД/памяти, участники связаны через `group_members`. При присоединении по ссылке (`?join=...`) автоматически заполняется форма

---

### 12. **Блок "Криптовалюты"**
**Файл**: `web/src/App.vue` (строки 452-470)

**Функционал**:
- Выбор монеты из списка (BTC, ETH, BNB, SOL, TON, DOGE, ADA, XRP, DOT)
- Кнопка "Обновить"
- Таблица: Монета, Цена (USD), Изменение за 24ч (%)

**API**: CoinGecko API (клиентская сторона, прямой запрос из браузера)

**Технологии**: Fetch API, кэширование в памяти компонента (5 минут)

**Логика**: При загрузке/обновлении — запрос к `https://api.coingecko.com/api/v3/simple/price?ids=...&vs_currencies=usd&include_24hr_change=true`, отображение в таблице

---

## Подробное описание файлов кода

### Бэкенд (Java)

#### `src/com/acs/finance/Main.java`
**Назначение**: Точка входа приложения

**Функционал**:
- Парсинг аргументов командной строки (порт, по умолчанию 8080)
- Создание экземпляра `HttpServerApp`
- Регистрация shutdown hook (корректное завершение при Ctrl+C)
- Запуск сервера и блокировка главного потока

**Технологии**: Java `main()`, `Runtime.addShutdownHook()`

---

#### `src/com/acs/finance/HttpServerApp.java`
**Назначение**: HTTP-сервер, маршрутизация, обработка запросов

**Основные компоненты**:
- **`StaticHandler`** (внутренний класс): Раздача статических файлов из `web/public/` (SPA fallback на `index.html`)
- **`ApiHandler`** (внутренний класс): Обработка REST API запросов на `/api/*`
- **`handleEvents()`**: SSE endpoint `/api/events` (регистрация клиентов, отправка событий)
- **`tick()`**: Периодическая задача (каждые 10 секунд) — проверка напоминаний, отправка SSE событий

**Маршруты API**:
- `POST /api/register`, `POST /api/login`, `POST /api/logout`, `GET /api/me` — авторизация
- `POST /api/transaction/add`, `GET /api/transactions`, `POST /api/transaction/delete` — транзакции
- `POST /api/budget/set`, `GET /api/budget`, `POST /api/budget/delete` — бюджеты
- `GET /api/currency`, `GET /api/currency/convert`, `GET /api/currency/history` — валюты
- `POST /api/reminder/add`, `GET /api/reminders` — напоминания
- `POST /api/group/create`, `POST /api/group/join`, `POST /api/group/leave`, `GET /api/group/me`, `GET /api/group/peers` — группы
- `GET /api/reports`, `GET /api/recommendations` — отчеты и рекомендации
- `GET /api/categorizer/suggest`, `POST /api/categorizer/add` — автокатегоризация

**Технологии**: `HttpServer`, `ExecutorService` (пул потоков), `ScheduledExecutorService`, JSON сериализация (ручная через `StringBuilder`)

---

#### `src/com/acs/finance/auth/AuthService.java`
**Назначение**: Управление пользователями и сессиями

**Функционал**:
- Регистрация пользователя (проверка уникальности, хеширование пароля SHA-256)
- Авторизация (проверка пароля, создание сессии, возврат cookie `SID`)
- Проверка сессии (валидация `SID`, получение `userId`)
- Выход (удаление сессии)

**Хранение**: In-memory (`ConcurrentHashMap`) или PostgreSQL через `UserDao`, `SessionDao`

**Технологии**: `MessageDigest` (SHA-256), UUID для сессий, cookie-управление

---

#### `src/com/acs/finance/service/FinanceService.java`
**Назначение**: Бизнес-логика финансовых операций

**Функционал**:
- **Транзакции**: добавление, получение списка (с фильтрами), удаление
- **Бюджеты**: установка лимита, получение списка, удаление, автоматическое обновление `spent` при добавлении расхода
- **Цели**: установка цели, получение списка
- **Напоминания**: добавление, получение списка, проверка наступивших (для тикера)
- **Отчёты**: агрегация по категориям за период
- **Рекомендации**: простые правила (расходы > доходов, топ категории, подписки, превышения)

**Хранение**: In-memory (`ConcurrentHashMap`, `CopyOnWriteArrayList`) или PostgreSQL через DAO

**Технологии**: Потокобезопасные коллекции, агрегация данных

---

#### `src/com/acs/finance/service/CurrencyService.java`
**Назначение**: Получение курсов валют и конвертация

**Функционал**:
- Получение текущего курса (`getRate()`) — запрос к `exchangerate.host`, фолбэк на `frankfurter.app`, кэш 1 минута
- Конвертация (`convert()`) — умножение суммы на курс
- История курсов (`getHistory()`) — запрос к `exchangerate.host/timeseries`, фолбэк на `frankfurter.app`, если не поддерживается — индивидуальные запросы по дням

**Технологии**: `HttpURLConnection`, JSON парсинг (ручной), кэширование в памяти (`ConcurrentHashMap` с TTL)

---

#### `src/com/acs/finance/service/SseHub.java`
**Назначение**: Управление SSE подключениями и broadcast событий

**Функционал**:
- Регистрация клиента (`register()`) — сохранение `OutputStream` и `userId`
- Отправка события конкретному пользователю (`send()`) — запись в `OutputStream` в формате SSE
- Broadcast события всем (`broadcast()`) — отправка всем зарегистрированным клиентам
- Удаление клиента (`remove()`)

**Технологии**: `OutputStream`, формат SSE (`data: {...}\n\n`), `CopyOnWriteArrayList` для потокобезопасности

---

#### `src/com/acs/finance/service/AutoCategorizer.java`
**Назначение**: Автоматическое определение категории по описанию транзакции

**Функционал**:
- Подсказка категории (`suggest()`) — поиск ключевых слов в описании, возврат соответствующей категории
- Добавление правила (`addRule()`) — связка ключевое слово → категория

**Хранение**: In-memory (`HashMap`) или через DAO (если будет реализовано)

**Технологии**: Поиск подстроки (case-insensitive)

---

#### `src/com/acs/finance/service/GroupService.java`
**Назначение**: Управление группами и групповыми бюджетами

**Функционал**:
- Создание группы (`create()`) — генерация `groupId`, сохранение в БД/памяти
- Присоединение к группе (`join()`) — добавление пользователя в группу
- Выход из группы (`leave()`) — удаление пользователя из группы
- Получение информации о группе пользователя (`myShare()`) — возврат `groupId`
- Получение участников группы (`members()`) — список пользователей в группе
- Агрегация доходов/расходов участников (`peers()`) — сумма по каждому участнику за период

**Хранение**: PostgreSQL через `GroupDao` или in-memory

**Технологии**: UUID для групп, агрегация SQL или in-memory

---

#### `src/com/acs/finance/db/Db.java`
**Назначение**: Управление подключением к PostgreSQL

**Функционал**:
- Проверка конфигурации (`isConfigured()`) — наличие `DB_URL`, `DB_USER`, `DB_PASS` в ENV
- Получение подключения (`get()`) — создание `Connection` через JDBC (с пулом, если реализовано)
- Тест подключения (`testConnection()`) — проверка доступности БД

**Технологии**: JDBC (`java.sql.Connection`), `DriverManager`, переменные окружения

---

#### `src/com/acs/finance/dao/*.java`
**Назначение**: Data Access Object — слой доступа к данным

**Файлы**:
- **`UserDao.java`**: CRUD для таблицы `users` (регистрация, получение по username/id)
- **`SessionDao.java`**: CRUD для таблицы `sessions` (создание, проверка, удаление сессии)
- **`TransactionDao.java`**: CRUD для таблицы `transactions` (добавление, получение списка с фильтрами, удаление)
- **`BudgetDao.java`**: CRUD для таблицы `budgets` (upsert лимита, обновление `spent`, получение списка, удаление)
- **`ReminderDao.java`**: CRUD для таблицы `reminders` (добавление, получение списка, пометка как отправленное)
- **`GoalDao.java`**: CRUD для таблицы `goals` (upsert цели, получение списка)
- **`GroupDao.java`**: CRUD для таблиц `groups` и `group_members` (создание группы, присоединение, выход, получение участников)

**Технологии**: JDBC (`PreparedStatement`, `ResultSet`), SQL-запросы, UUID для первичных ключей

---

#### `src/com/acs/finance/model/*.java`
**Назначение**: Модели данных (POJO)

**Файлы**:
- **`User.java`**: `id` (UUID), `username`, `passwordHash`
- **`Transaction.java`**: `id`, `userId`, `date` (LocalDate), `category`, `description`, `amount` (double)
- **`Budget.java`**: `userId`, `category`, `limit`, `spent`
- **`Reminder.java`**: `id`, `userId`, `dueDate` (LocalDate), `message`, `amount` (опционально), `sent` (boolean)
- **`Goal.java`**: `id`, `userId`, `name`, `targetAmount`, `currentAmount`
- **`CurrencyRates.java`**: `base`, `rates` (Map<String, Double>)

**Технологии**: Plain Java классы, `java.time.LocalDate`, `java.util.UUID`

---

### Фронтенд (Vue 3 + TypeScript)

#### `web/src/App.vue`
**Назначение**: Главный компонент приложения (SPA)

**Структура**:
- **Template**: Все UI блоки (см. раздел "Подробное описание UI блоков")
- **Script**: Вся бизнес-логика фронтенда
  - Реактивные данные (`ref`, `computed`)
  - Функции авторизации, работы с транзакциями, бюджетами, графиками, рекомендациями и т.д.
  - Интеграция с API через `api()` из `client.ts`
  - Интеграция с SSE через `sseConnect()` из `client.ts`
  - Управление Chart.js для графиков
- **Style**: Scoped стили (частично, основные стили в `web/public/styles.css`)

**Технологии**: Vue 3 Composition API, TypeScript, Chart.js, CSS

**Размер**: ~800 строк кода

---

#### `web/src/main.ts`
**Назначение**: Точка входа Vue-приложения

**Функционал**:
- Создание Vue-приложения (`createApp(App)`)
- Подключение store (`use(store)`)
- Монтирование в `#app` (из `index.html`)
- Регистрация Service Worker (PWA)

**Технологии**: Vue 3 `createApp()`, Service Worker API

---

#### `web/src/api/client.ts`
**Назначение**: API-клиент с офлайн-кэшированием и SSE

**Функционал**:
- **`api()`**: Универсальная функция для HTTP-запросов
  - GET-запросы кэшируются в IndexedDB (ключ: URL + query params)
  - При офлайн-режиме — возврат из кэша
  - POST/PUT/DELETE — всегда на сервер (очередь не реализована)
- **`sseConnect()`**: Подключение к SSE endpoint, обработка событий, вызов колбэков
- **`suggestCategory()`**: Вспомогательная функция для автоподсказки категории
- **`exportTransactionsCsv()`**: Генерация CSV из массива транзакций, скачивание файла

**Технологии**: Fetch API, IndexedDB (через `idb` или нативный API), EventSource (SSE), Blob API (CSV)

---

#### `web/src/store/index.ts`
**Назначение**: Глобальное состояние приложения (Vuex-like store)

**Данные**:
- `user` — текущий пользователь (из `/api/me`)
- `theme` — тема (dark/light), синхронизируется с LocalStorage и `document.documentElement`
- `favorites` — Set избранных категорий, синхронизируется с LocalStorage

**Методы**:
- `setUser()`, `setTheme()`, `toggleFavorite()` — обновление состояния и синхронизация с LocalStorage

**Технологии**: Vue 3 `reactive()`, `ref()`, LocalStorage API

---

#### `web/index.html`
**Назначение**: HTML-шаблон приложения

**Содержимое**:
- Мета-теги (viewport, charset)
- Точка монтирования Vue (`<div id="app"></div>`)
- Подключение скрипта (`<script type="module" src="/src/main.ts"></script>`)
- Регистрация Service Worker

**Технологии**: HTML5, Service Worker API

---

#### `web/vite.config.ts`
**Назначение**: Конфигурация Vite (сборщик)

**Настройки**:
- `base: '/'` — базовый путь
- `plugins: [vue()]` — плагин Vue
- `build.outDir: './public'` — выходная директория (собранные файлы)
- `server.port: 5173` — порт dev-сервера
- `server.proxy: { '/api': 'http://localhost:8080' }` — проксирование API в dev-режиме

**Технологии**: Vite, Vue plugin

---

#### `web/public/sw.js`
**Назначение**: Service Worker для PWA (офлайн-кэширование)

**Функционал**:
- Кэширование статических файлов (HTML, CSS, JS, изображения)
- Стратегия: Cache First (если есть в кэше — отдаём, иначе — сеть)
- Обновление кэша при изменении версии

**Технологии**: Service Worker API, Cache API

---

#### `web/public/styles.css`
**Назначение**: Глобальные стили приложения

**Содержимое**:
- CSS-переменные для темной/светлой темы (цвета, фоны, границы)
- Стили для всех UI компонентов (header, формы, таблицы, графики, кнопки)
- Адаптивная верстка (Grid, Flexbox)
- Анимации (toast, transitions)

**Технологии**: CSS3, CSS-переменные, Grid, Flexbox

---

#### `web/tsconfig.json`
**Назначение**: Конфигурация TypeScript

**Настройки**: Целевая версия ES2020, модули ES, строгий режим, пути для импортов

---

#### `web/package.json`
**Назначение**: Зависимости и скрипты npm

**Зависимости**:
- `vue` — Vue 3
- `chart.js` — библиотека графиков
- `vite` — сборщик
- `@vitejs/plugin-vue` — плагин Vue для Vite

**Скрипты**:
- `dev` — запуск dev-сервера (Vite)
- `build` — сборка production (Vite build)

---

### База данных

#### `db/schema.sql`
**Назначение**: SQL-схема для PostgreSQL

**Таблицы**:
- `users` — пользователи (id UUID, username, password_hash)
- `sessions` — сессии (id UUID, user_id, expires_at)
- `transactions` — транзакции (id UUID, user_id, date_epoch_day, category, description, amount)
- `budgets` — бюджеты (user_id, category, limit_amount, spent) — составной первичный ключ
- `goals` — цели (id UUID, user_id, name, target_amount, current_amount)
- `reminders` — напоминания (id UUID, user_id, due_date, message, amount, sent)
- `groups` — группы (id UUID, name)
- `group_members` — участники групп (group_id, user_id) — составной первичный ключ

**Технологии**: PostgreSQL, UUID, индексы, внешние ключи (если реализованы)

---

## API Endpoints (полный список)

### Авторизация
- `POST /api/register` — регистрация (form: `username`, `password`)
- `POST /api/login` — вход (form: `username`, `password`) → cookie `SID`
- `POST /api/logout` — выход
- `GET /api/me` — информация о текущем пользователе

### Транзакции
- `POST /api/transaction/add` — добавление (form: `date`, `amount`, `category?`, `description?`)
- `GET /api/transactions?from=...&to=...&category=...` — список с фильтрами
- `POST /api/transaction/delete` — удаление (form: `id`)

### Бюджеты
- `POST /api/budget/set` — установка лимита (form: `category`, `limit`)
- `GET /api/budget` — список бюджетов
- `POST /api/budget/delete` — удаление лимита (form: `category`)

### Валюты
- `GET /api/currency?base=...&symbols=...` — текущий курс
- `GET /api/currency/convert?from=...&to=...&amount=...` — конвертация
- `GET /api/currency/history?from=...&to=...&base=...&symbols=...` — история курсов

### Напоминания
- `POST /api/reminder/add` — добавление (form: `dueDate`, `message`, `amount?`)
- `GET /api/reminders` — список

### Группы
- `POST /api/group/create` — создание (form: `name?`)
- `POST /api/group/join` — присоединение (form: `groupId`)
- `POST /api/group/leave` — выход
- `GET /api/group/me` — информация о группе пользователя
- `GET /api/group/peers?from=...&to=...` — доходы/расходы участников

### Отчёты и рекомендации
- `GET /api/reports?period=...` — отчёты (агрегация по категориям)
- `GET /api/recommendations` — рекомендации (простые правила)

### Автокатегоризация
- `GET /api/categorizer/suggest?desc=...` — подсказка категории
- `POST /api/categorizer/add` — добавление правила (form: `keyword`, `category`)

### Реалтайм
- `GET /api/events` — SSE поток (события: `tx-added`, `budget-update`, `reminder`, `alert`)

### Health
- `GET /health` — проверка работоспособности
- `GET /health/db` — проверка подключения к БД

---

## Запуск (быстро)

Требуется: Java 17+ (подойдёт и 11), Node.js 16+.

### 1. Скомпилировать Java
```bash
javac -d out $(dir /s /b src\\*.java)
```
(На PowerShell: `Get-ChildItem -Recurse src/*.java | ForEach-Object { $_.FullName } | %{ $_ } | javac -d out @-`)

### 2. Собрать фронтенд (Vue → статика в `web/public/`)
```bash
cd web
npm install
npm run build
```

### 3. Запустить сервер
```bash
java -cp "out;lib/postgresql-42.7.8.jar" com.acs.finance.Main 8080
```

Откройте `http://localhost:8080`.

### Режим разработки фронтенда
- Терминал 1 (сервер): `java -cp "out;lib/postgresql-42.7.8.jar" com.acs.finance.Main 8080`
- Терминал 2 (фронт): 
  ```bash
  cd web
  npm run dev
  ```
  Откройте `http://localhost:5173` (прокси на `/api` → `localhost:8080`).

---

## Подключение PostgreSQL (опционально)

Приложение автоматически переключится на БД, если заданы переменные `DB_URL`, `DB_USER`, `DB_PASS`.

### 1. Установи PostgreSQL и создай БД/пользователя

### 2. Применить схему
```bash
psql -h localhost -U finuser -d findb -f db/schema.sql
```

### 3. Скачать JDBC‑драйвер и запустить с ним (PowerShell)
```powershell
$JAR="lib/postgresql-42.7.8.jar"
if (!(Test-Path $JAR)) { New-Item -ItemType Directory -Force -Path lib | Out-Null; Invoke-WebRequest -OutFile $JAR https://jdbc.postgresql.org/download/postgresql-42.7.8.jar }
$env:DB_URL="jdbc:postgresql://localhost:5432/findb"; $env:DB_USER="finuser"; $env:DB_PASS="finpass"
javac -encoding UTF-8 -cp $JAR -d out (Get-ChildItem -Recurse -Filter *.java src | % FullName)
java -cp "out;$JAR" com.acs.finance.Main 8080
```

Используемые таблицы: `users`, `sessions`, `transactions`, `budgets`, `goals`, `reminders`, `groups`, `group_members`.
Если переменные не заданы — используется in-memory режим (как ранее).

### Полное пересоздание БД и таблиц (если всё удалили)
1) Подключитесь как суперпользователь (например, `postgres`) и выполните:
```sql
DROP DATABASE IF EXISTS findb;
DROP USER IF EXISTS finuser;
CREATE DATABASE findb ENCODING 'UTF8';
CREATE USER finuser WITH ENCRYPTED PASSWORD 'finpass';
GRANT ALL PRIVILEGES ON DATABASE findb TO finuser;
```
2) Включите права на схему `public`:
```sql
\c findb
GRANT ALL ON SCHEMA public TO finuser;
```
3) Примените схему таблиц:
```bash
psql -h localhost -U finuser -d findb -f db/schema.sql
```
4) Запустите сервер с ENV:
```powershell
$env:DB_URL="jdbc:postgresql://localhost:5432/findb"
$env:DB_USER="finuser"
$env:DB_PASS="finpass"
java -cp "out;lib/postgresql-42.7.8.jar" com.acs.finance.Main
```
Примечание: в `db/schema.sql` используется `pgcrypto` (если требуется) — выполняйте под ролью с правами на `CREATE EXTENSION`, либо заранее включите расширение.

---

## Шпаргалка команд (Windows PowerShell)

1) Полная пересборка Java (из корня проекта):
```powershell
Remove-Item -Recurse -Force out -ErrorAction SilentlyContinue
$JAR="lib/postgresql-42.7.8.jar"
javac -encoding UTF-8 -cp $JAR -d out (Get-ChildItem -Recurse -Filter *.java src | % FullName)
```

2) Пересборка фронтенда (Vite → web/public):
```powershell
cd web
npm install
# если npm.ps1 заблокирован политикой:
# Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
# или
# cmd /c "npm run build"
npm run build
cd ..
```

3) Запуск сервера без БД:
```powershell
java -cp "out;lib/postgresql-42.7.8.jar" com.acs.finance.Main 8080
```

4) Создание БД/пользователя и схема:
```powershell
psql -U postgres -c "DROP DATABASE IF EXISTS finappdb;"
psql -U postgres -c "DROP USER IF EXISTS finapp;"
psql -U postgres -c "CREATE USER finapp WITH ENCRYPTED PASSWORD 'StrongPass_ChangeMe123!';"
psql -U postgres -c "CREATE DATABASE finappdb ENCODING 'UTF8' OWNER finapp;"
psql -U postgres -d finappdb -c "GRANT ALL ON SCHEMA public TO finapp;"
psql -h localhost -U finapp -d finappdb -f db/schema.sql
```

5) Запуск сервера с БД:
```powershell
$env:DB_URL="jdbc:postgresql://localhost:5432/finappdb"
$env:DB_USER="finapp"
$env:DB_PASS="finpass"
java -cp "out;lib/postgresql-42.7.8.jar" com.acs.finance.Main 8080
```

6) Дев‑режим (прокси фронта на 5173):
```powershell
# окно 1
java -cp "out;lib/postgresql-42.7.8.jar" com.acs.finance.Main 8080
# окно 2
cd web
npm run dev
```

---

## Производительность и многопоточность

- **Пул потоков**: `HttpServer#setExecutor(Executors.newFixedThreadPool(...))` — параллельная обработка клиентов
- **Потокобезопасные структуры**: `ConcurrentHashMap`, `CopyOnWriteArrayList`
- **SSE**: каждое подключение держит свой поток вывода; события отправляются выборочно
- **Тикер**: `ScheduledExecutorService` каждые 10 секунд — раздаёт напоминания

---

## Подводные камни и советы

- **PowerShell**: переменные среды задаются через `$env:VAR="..."` (не Bash‑синтаксис)
- **Сборка с JDBC**: важно добавлять JAR в `-cp` и при `javac`, и при `java`
- **Кэш браузера**: при обновлении фронта делайте `Ctrl+F5`
- **Ошибка `user_exists_or_bad`**: используйте уникальный логин и убедитесь, что сервер запущен с JDBC/ENV (иначе in‑memory)
- **Проверка БД**: `SELECT id, username FROM users; SELECT COUNT(*) FROM transactions;`
- **Service Worker**: при обновлении фронта может потребоваться очистка кэша (DevTools → Application → Service Workers → Unregister)

---

## Логирование

Приложение использует встроенный в JDK `java.util.logging` (JUL) для логирования.

### Особенности

- **Двойной вывод**: логи пишутся в консоль и в файл `logs/app.log`
- **Ротация файлов**: автоматическая ротация при достижении 10MB (до 5 файлов)
- **Уровни логирования**:
  - `INFO` — важные события (запуск/остановка сервера, регистрация, вход)
  - `WARNING` — предупреждения (неудачные попытки входа, превышение бюджета)
  - `ERROR` — ошибки (исключения, проблемы с БД, внешними API)
  - `DEBUG` — отладочная информация (HTTP-запросы, операции с БД)
  - `TRACE` — детальная информация (использование кэша)

### Что логируется

- Запуск и остановка сервера
- HTTP-запросы (метод, путь, IP-адрес, статус ответа)
- Авторизация и регистрация пользователей
- Финансовые операции (добавление/удаление транзакций)
- Операции с базой данных
- Запросы к внешним API (валюты)
- Ошибки и исключения

### Просмотр логов

```bash
# Просмотр последних записей
tail -f logs/app.log

# Поиск ошибок
grep ERROR logs/app.log

# Поиск по пользователю
grep "user=username" logs/app.log
```

---

## Что дальше (идеи)

- Пароли: заменить SHA‑256 на bcrypt/argon2
- Миграции схемы (Flyway/Liquibase) и тесты DAO
- Пагинация истории, продвинутые отчёты/ML‑категоризация, офлайн‑очередь на запись
- ИИ‑рекомендации через внешний API (OpenAI/локальный LLM)
- Экспорт PDF отчётов
- Мобильное приложение (PWA уже готов, можно доработать)

---

## Структура проекта

```
ACS/
├── src/com/acs/finance/          # Java исходники
│   ├── Main.java                  # Точка входа
│   ├── HttpServerApp.java         # HTTP-сервер, маршрутизация
│   ├── auth/                      # Авторизация
│   │   └── AuthService.java
│   ├── service/                   # Бизнес-логика
│   │   ├── FinanceService.java    # Транзакции, бюджеты, цели, напоминания
│   │   ├── CurrencyService.java   # Валюты
│   │   ├── SseHub.java            # SSE
│   │   ├── AutoCategorizer.java   # Автокатегоризация
│   │   └── GroupService.java      # Группы
│   ├── dao/                       # Data Access Object
│   │   ├── UserDao.java
│   │   ├── SessionDao.java
│   │   ├── TransactionDao.java
│   │   ├── BudgetDao.java
│   │   ├── ReminderDao.java
│   │   ├── GoalDao.java
│   │   └── GroupDao.java
│   ├── model/                     # Модели данных
│   │   ├── User.java
│   │   ├── Transaction.java
│   │   ├── Budget.java
│   │   ├── Reminder.java
│   │   ├── Goal.java
│   │   └── CurrencyRates.java
│   ├── db/
│   │   └── Db.java                # Подключение к PostgreSQL
│   └── util/
│       └── Logger.java            # Утилита логирования
├── web/                           # Фронтенд (Vue 3)
│   ├── src/
│   │   ├── App.vue                # Главный компонент (все UI блоки)
│   │   ├── main.ts                # Точка входа Vue
│   │   ├── api/
│   │   │   └── client.ts          # API-клиент, SSE, IndexedDB
│   │   └── store/
│   │       └── index.ts           # Глобальное состояние
│   ├── public/                    # Собранные файлы (Vite build)
│   │   ├── index.html
│   │   ├── styles.css
│   │   ├── sw.js                  # Service Worker
│   │   └── assets/               # JS/CSS бандлы
│   ├── index.html                 # Исходный HTML
│   ├── vite.config.ts             # Конфигурация Vite
│   ├── tsconfig.json              # Конфигурация TypeScript
│   └── package.json               # Зависимости npm
├── db/
│   └── schema.sql                 # SQL-схема PostgreSQL
├── lib/
│   └── postgresql-42.7.8.jar     # JDBC драйвер
├── out/                           # Скомпилированные .class файлы (не коммитить)
├── logs/                          # Логи приложения (logs/app.log, не коммитить)
└── README.md                      # Эта документация
```

---

## Лицензия

Проект создан в учебных целях.
